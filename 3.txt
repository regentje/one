Написать функцию для реверсии слова не используя встроенные методы:

let reverce = function (word){
  let rever = '';
 for (let i=word.length-1; i>=0; i--){
   rever += word[i];
 }
 return rever;
}
console.log(reverce('think'));